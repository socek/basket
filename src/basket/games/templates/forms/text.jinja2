<div class="form-group">
    {% include '_error.jinja2' %}

    <input
        class="form-control"
        value="{{value}}"
        name="{{name}}"
        id="{{id}}"
        {%- if disabled %} disabled=disabled {% endif -%}
        >
</div>
